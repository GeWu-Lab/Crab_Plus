# ============================================================
# Crab-Qwen2.5-Omni Requirements
# ============================================================
# Note: Install PyTorch first with the correct CUDA version:
#   pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu121

# Core Dependencies
transformers==4.51.3
accelerate==1.10.1
deepspeed==0.16.4
peft==0.10.0

# Qwen2.5-Omni Utils (with decord video backend)
qwen-omni-utils[decord]

# Data Processing
numpy==1.26.3
opencv-python==4.11.0.86
Pillow
einops
pytz
jsonlines
cloudpickle
ml-dtypes
pyarrow==20.0.0
moviepy==1.0.3
librosa
audioread
av

# Distributed Training
tensorboard

# ============================================================
# SAM2 (for segmentation tasks only)
# Install separately:
#   cd sam2 && pip install -e ".[notebooks]"
# ============================================================
